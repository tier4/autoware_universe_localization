## autoware_carla_interface

This repository split mirrors `main` branch of `autowarefoundation/autoware_universe:/simulator/autoware_carla_interface` into `awf-latest` branch, used for TIER IV release workflows.

This branch only holds workflow files.
